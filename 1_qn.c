#include <stdio.h>

int main()
{
     float a,b ;
    printf("please enter the first value i.e a  \n ");
    scanf("%f", &a);

    printf("please enter the second value i.e  b \n ");
    scanf("%f", &b);

    printf("sum of a and b is %f \n ", a + b );
    printf("diff  of a and b is %f \n", a - b );
     printf("multi of a and b is %f \n", a * b );
     printf("div of a and b is %f\n ", a / b );
    return 0;
}